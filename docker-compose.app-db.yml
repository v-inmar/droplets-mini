services:
  db_app:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: droplet_appdb
    ports:
      - "5432:5432"
    
    networks:
      - app_network

networks:
  app_network:
    external: true